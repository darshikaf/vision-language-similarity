name: vision-language-similarity-integration-test
services:
  evaluator:
    image: ${TEST_IMAGE_NAME}
    command: ["scripts/run_test.sh", "--test-suite", "integration"]
    environment:
      - PYTHONPATH=/app
    volumes:
      - ./test-reports:/tmp/test-reports
      - model_cache:/root/.cache/openclip

volumes:
  model_cache: